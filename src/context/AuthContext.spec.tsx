import React from 'react';
//import { Provider as AuthProvider, Context as AuthContext } from './AuthContext';

describe('login', () => {
  it('it sets jwt token in state', () => {
    const TestComponent = () => {
      //const { signin, signout, isSignedIn, errorMessage, token } = React.useContext(AuthContext);
      return <div></div>;
    };
  });
});
